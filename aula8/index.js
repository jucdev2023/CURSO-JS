//primeira pergunta que vc vai precisar fazer antes do exercico
// se alguma informação vai mudar
//se nao for mundar, use const
//fiz usando variavel constante
const nome = 'Luiz otavio';
const sobrenome = 'miranda';
const idade = 30;
const peso = 84;
const alturaM = 1.80;
let indiceDeMassaCorporal;
indiceDeMassaCorporal = peso / (alturaM*alturaM);
let anoNascimento;
anoNascimento = 1993-idade;
console.log(`${nome} ${sobrenome} tem ${idade}anos, e pesa ${peso} kg`);
console.log (`tem ${alturaM}de altura, e seu imc é: ${indiceDeMassaCorporal}`);
console.log(`${nome} nasceu em ${anoNascimento}`);

 // + 

   /* 
     exemplo: 
     console.log(nome +' ' + sobrenome + ' tem')
     

     tem tbm o template string, onde eu posso colocar variaveis dentro de uma string usa  ${ } 
   */

